<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="io.spring.infrastructure.mybatis.readservice.TaskNoteReadService">
    <sql id="selectTaskNote">
        select C.id as id,
               C.body as body,
               C.task_id as taskId,
               C.user_id as userId,
               C.created_at as createdAt,
               C.updated_at as updatedAt,
               U.username as userUsername,
               U.bio as userBio,
               U.image as userImage
        from task_notes C
                 left join users U on C.user_id = U.id
    </sql>

    <select id="findById" resultMap="taskNote">
        <include refid="selectTaskNote"/>
        where C.id = #{id}
    </select>

    <select id="findByTaskId" resultMap="taskNote">
        <include refid="selectTaskNote"/>
        where C.task_id = #{taskId}
        order by C.created_at
    </select>

    <resultMap id="taskNote" type="io.spring.application.data.TaskNoteData">
        <id column="id" property="id"/>
        <result column="body" property="body"/>
        <result column="taskId" property="taskId"/>
        <result column="userId" property="userId"/>
        <result column="createdAt" property="createdAt"/>
        <result column="updatedAt" property="updatedAt"/>
        <association property="profileData" javaType="io.spring.application.data.ProfileData">
            <result column="userId" property="id"/>
            <result column="userUsername" property="username"/>
            <result column="userBio" property="bio"/>
            <result column="userImage" property="image"/>
        </association>
    </resultMap>
</mapper>
